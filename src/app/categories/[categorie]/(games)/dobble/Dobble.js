"use client";

export default function Dobble({ roomId, roomToken, user, gameData }) {
  console.log("roomId", roomId);
  console.log("roomToken", roomToken);
  console.log("user", user);
  console.log("gameData", gameData);
  return <div>coucou dobble</div>;
}
